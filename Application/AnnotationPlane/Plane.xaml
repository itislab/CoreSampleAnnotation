<UserControl x:Class="CoreSampleAnnotation.AnnotationPlane.Plane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoreSampleAnnotation.AnnotationPlane"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--<Slider Orientation="Horizontal" Name="ScaleSlider" Minimum="10" Maximum="3000" Value="{Binding Path=ColScaleController.ScaleFactor, Mode=TwoWay}"></Slider>-->
        <local:AnnotationGrid x:Name="AnnoGrod" Grid.Row="1" DataContext="{ Binding AnnoGridVM }" >
        </local:AnnotationGrid>
        <local:ClassificationView x:Name="ClassificationView" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30" DataContext="{Binding classificationVM}">
        </local:ClassificationView>
    </Grid>
</UserControl>

<UserControl x:Class="AnnotationPlane.LayeredColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnnotationPlane"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:LengthLayerVM}">
            <TextBlock Text="{Binding Length, StringFormat={}{0} м}" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        </DataTemplate>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding Layers}" Height="{Binding ColumnHeight}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border BorderThickness="1" BorderBrush="Black" Height="{Binding Length}">
                    <ContentPresenter Content="{Binding}" />
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
